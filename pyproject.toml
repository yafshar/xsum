[build-system]
requires = [
	"setuptools>=64",
	"wheel",
	"pybind11>=2.5.0",
	"versioneer[toml]>=0.29",
]
build-backend = "setuptools.build_meta"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "xsum/_version.py"
tag_prefix = "v"
parentdir_prefix = "xsum-"
